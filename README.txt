# Streamlit Catalogue App

## Run Locally
1) Install Python 3.10+
2) Open terminal in this folder
3) Install requirements:
   pip install -r requirements.txt

4) Run app:
   streamlit run app.py

It will open:
http://localhost:8501
